# Backlog

Ideas evaluated but not built. Each carries its measured evidence and a pass/fail
gate set *before* building, so revisiting starts from numbers, not enthusiasm.

| # | File | Status | One line |
|---|---|---|---|
| 001 | `001-yahoo-rank-divergence.md` | BLOCKED on data | Flag where Yahoo's ordering disagrees with ours -> predicts what the nine opponents do. **No Yahoo ranking data exists in the repo** — both Yahoo-named files are our own export. Needs a user paste carrying `XRank`/`ADP`. Season-long value. |
